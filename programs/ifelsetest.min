funct main.

params {
} params

locals {
  m : digit.
  n : digit.
  o : digit.
} locals

body {
  read(n).
  such n greaterorequal 100 {
    shout(n).
  }
  next {
    m = n sub 1.
    shout(m).
  }

shout(o).

} body
